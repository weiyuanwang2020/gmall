<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.atguigu.gmall.activity.mapper.ActivityInfoMapper">

    <resultMap id="ActivityRuleMap" type="com.atguigu.gmall.model.activity.ActivityRule" autoMapping="true">
    </resultMap>


    <select id="selectExistSkuIdList" resultType="java.lang.Long">
        select
            sku.sku_id
        from activity_info info
        inner join activity_sku sku on sku.activity_id = info.id
        <where>
            <if test="skuIdList != null">
                sku.sku_id in
                <foreach collection="skuIdList" item="skuId" open="(" close=")" separator=",">
                    #{skuId}
                </foreach>
            </if>
            and now() between info.start_time and info.end_time
        </where>
    </select>
    <select id="selectActivityRuleList" resultMap="ActivityRuleMap">
        select
            info.activity_type,
            rule.id,rule.activity_id,
            rule.condition_amount,
            rule.condition_num,
            rule.benefit_amount,
            rule.benefit_discount,
            rule.benefit_level
        from activity_info info
        inner join activity_sku sku on sku.activity_id = info.id
        inner join activity_rule rule on rule.activity_id = info.id
        where
            sku.sku_id = #{skuId}
        and
            now() between info.start_time and info.end_time
        order by rule.condition_amount desc, rule.condition_num desc
    </select>
    <select id="selectCartActivityRuleList" resultMap="ActivityRuleMap">
        select
            info.activity_type as activityType,
            rule.id,rule.activity_id,
            rule.condition_amount,
            rule.condition_num,
            rule.benefit_amount,
            rule.benefit_discount,
            rule.benefit_level,
            sku.sku_id as skuId
        from activity_info info
        inner join activity_sku sku on sku.activity_id = info.id
        inner join activity_rule rule on rule.activity_id = info.id
        <where>
            sku.sku_id in
            <foreach collection="skuIdList" separator="," close=")" open="(" item="skuId">
                #{skuId}
            </foreach>
            and now() between info.start_time and info.end_time
        </where>
        order by rule.condition_amount desc, rule.condition_num desc
    </select>
</mapper>
